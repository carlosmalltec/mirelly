import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:sistema_de_controle/app/modules/providers/providers_page.dart';

void main() {
  testWidgets('ProvidersPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProvidersPage(title: 'Providers')));
    //  final titleFinder = find.text('Providers');
    //  expect(titleFinder, findsOneWidget);
  });
}
